name: Aggregate batches and deploy Pages

on:
  repository_dispatch:
    types:
      - archive-batch-submitted
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "site/**"
      - "index/**"
      - "package.json"
      - "package-lock.json"
      - "vite.config.ts"
      - "tsconfig.json"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: archive-aggregate-and-pages
  cancel-in-progress: false

jobs:
  aggregate:
    runs-on: ubuntu-latest
    outputs:
      page_artifact: ${{ steps.upload.outputs.artifact_id }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-ci.txt

      - name: Install index dependencies
        run: python -m pip install -r requirements-ci.txt

      - name: Aggregate all pending batch refs
        env:
          ARCHIVE_MEDIA_PAT: ${{ secrets.ARCHIVE_MEDIA_PAT }}
        run: python tools/aggregate_batches.py --index-root .

      - name: Verify generated index
        run: python tools/verify_index.py .

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Build static gallery
        run: |
          npm ci
          npm run build

      - uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        id: upload
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

  deploy:
    needs: aggregate
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
